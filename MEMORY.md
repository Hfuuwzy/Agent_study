# 项目记忆文档

> 面向 Agent 的长期约定、不可违反的红线与不可重复的教训。完整流程和环境知识由 `docs/` 承载，并在新会话启动时自动加载。

## 六大总原则

1. **学习优先，推送只是记录。**“学习第 X 章”只做 README 和教程代码准备；“完成/学完/结束第 X 章”才生成 NOTES、建分支并推送。
2. **不提前生成 NOTES，不提前推送。**
3. **不擅自安装依赖、运行安装命令或改动环境。**除非用户明确要求。
4. **不擅自扩展功能或学习范围。**
5. **提取教程代码，不自行发明。**“拉取代码”是从 Hello-Agents 教程 Markdown 或官方示例提取，不是按 API 文档重写等价实现。
6. **凭据不进仓库。**密钥仅存未纳入版本控制的 `.env` 或环境变量，不写入源码、注释、文档或提交信息。

## 关键红线

- 每章基于同步的 main 创建独立 `chapterX` 分支，通过 GitHub PR 合并；禁止直推 main 或本地 `git merge chapterX`。
- 提交前必须执行 `git diff --staged --stat`，确保只暂存本章内容和必要的全局文档变更；禁止带入工作区其他变更。
- 删除或重建远程分支只能使用 `--force-with-lease`，禁止裸 `--force`。
- 先检查 `chapterX/code/` 是否已有用户提供的参考文件；拓展已有包时只补教程要求的文件，不本地重写框架，也不因缺依赖写 mock。
- Kimi K2.6 请求只保留 `model + messages`；不传 `temperature`、`top_p`、`presence_penalty`、`frequency_penalty`，不回退到 GPT 模型。

## 不可重复的教训

### 代码与流程

- “拉取代码”被误解为自创实现、忽略参考文件或过度包装，都会造成教程对齐失败；实现前先判定“从零构建”还是“基于已有包拓展”。
- 章节分支混入其他未提交章节文件会污染 PR；创建分支前同步 main，提交前核验暂存区。
- 学习阶段的 NOTES 和推送会打断学习节奏；只有用户明确结束本章时才进入完成流程。

### 环境与运行

- 所有使用 `hello_agents` 的脚本都必须在其 import 前执行 `load_dotenv()`；否则框架会以空配置初始化 Qdrant 连接。
- 中文 ContextBuilder 相关性评分可能被空格分词全过滤；需要 `ContextConfig(min_relevance=0.0)` 时，检查所有实例化点。
- Windows 控制台遇到 emoji 输出的 GBK 编码错误时，在 PowerShell 设置 `$env:PYTHONIOENCODING="utf-8"`。
- 凭据泄露后，替换源码中的值并不等于凭据安全；用户仍须到对应平台撤销或轮换。

### 记忆保护

- 禁止用旧 commit 覆盖 `MEMORY.md`，除非用户明确要求回滚。
- 编辑本文件前先完整读取；编辑后核验“六大总原则”“关键红线”“不可重复的教训”“参考文档”四节均存在。
- 不能从文件开头判断记忆是否恢复；缺失时优先从会话记录恢复，而不是假设旧 commit 正确。

## 参考文档

| 需要了解什么 | 完整文档 |
|---|---|
| 章节进度与项目概览 | `README.md` |
| 学习/完成流程、Git、README/NOTES、代码对齐 | `docs/STUDY_WORKFLOW.md` |
| `.env`、Kimi、依赖版本、`load_dotenv`、中文检索、编码与排障 | `docs/ENVIRONMENT.md` |

**最后更新**：2026-08-25
