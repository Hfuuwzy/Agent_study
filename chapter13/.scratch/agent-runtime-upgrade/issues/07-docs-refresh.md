# 07: 文档刷新

**What to build:** 刷新两级 README：根 README 章节表推进到 Ch13；chapter13 README 增补"升级"段落——Run 状态机、SSE 事件流、失败/降级语义三张图，使 README 能独立讲清系统（面试叙事成立）。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] 根 README 章节表刷新至 Ch13，升级事实与 ADR 结论同步
- [x] chapter13 README 增补升级段：状态机 / 事件流 / 失败语义三图
- [x] README 能独立讲清系统：一个没看过代码的人读完能复述架构与关键权衡

## Answer

### 实现内容

- **根 README（`README.md`）**：
  - 章节概览表新增第十二章（性能评估，⏭️ 已跳过）与第十三章（✅ 已完成）两行；
  - 各章节详细说明新增「第十三章：智能旅行助手」段落：四专职 Agent + 共享高德 MCPTool 概述、升级要点六条（受理执行分离 / 状态机 / SSE 真实进度 / 显式降级 / 每步独立重试 / 中间结果类型化 + 输入隔离）、ADR 0001-0003 设计权衡结论、关键代码路径；
  - 项目结构树补入 `chapter13/` 分支（README/CONTEXT/ADR/code）；
  - 更新记录补 2026-09-23 条目。
- **chapter13 README（`chapter13/README.md`）** 新增第 9 节「Agent 运行时升级」：
  - **9.1** 旧实现"不可信"三处硬伤对照（伪造兜底 / 假进度 / 错城市图片）；
  - **9.2 Run 状态机（第一张图）**：`pending → running → success | degraded | failed` ASCII 状态图 + 三终态判定逻辑；
  - **9.3 SSE 真实事件流（第二张图）**：受理-订阅-后台执行时序 ASCII 图 + 六类事件载荷表 + 工程细节（历史重放/线程模型/心跳保活）；
  - **9.4 失败与降级语义（第三张图）**：四步流水线失败分支 ASCII 图 + 每步独立重试 / 先失败后成功也不吞错 / 显式降级空壳 / 图片兜底中性化 / 请求层快速拒绝；
  - **9.5 设计权衡（ADR 0001-0003）**：结论 + 一句话面试论述 + 已知局限；
  - **9.6 评测与验证**：5 条黄金评测集 + 离线 CLI + 真实 Key 快乐路径 + 73/73 回归。
- **历史段落 reconcile**：4.1 fallback、4.3 前端细节（Axios 50 分钟超时 / 模拟进度条）、6.1 api.ts、7 冒烟路径、8 学习建议 中与升级冲突的旧描述均加 ~~strike~~ 标注并指到第 9 节，避免新旧叙事打架。

### 验证证据

- 无代码改动，纯 Markdown 文档刷新，无 typecheck / 单测 / 全量回归适用（未触碰 `backend/` 或 `frontend/`）。
- 已人工通读：根 README 章节表推进至 Ch13、升级事实与 ADR 结论同步；chapter13 README 第 9 节含三张 ASCII 图且能独立讲清 Run/事件流/失败语义；新旧段落引用一致性核对（`9.x` 指向、思考题答案定型）。
- 独立叙事检验：第 9 节从「为什么不可信」→「状态机」→「事件流」→「失败语义」→「设计权衡」→「评测」线性展开，未看过代码者可复述架构与关键权衡。
